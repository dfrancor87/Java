/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package uf3.listaEjercicios;

/**
 *
 * @author Danfer
 */
public class pol {/*
    System.getProperty("line.separator");

Leer con Scanner:

File f = new File("\\C...");
Scanner s;

s = new Scanner(f);
while (s.hasNextLine()) {
	String linea = s.nextLine();
}
s.close();


Escribir con file writer:

FileWriter fichero;
PrintWriter pw;

fichero = new FileWriter("destino.txt",true); //true no sobrescribir
pw = new PrintWriter(fichero);
pw.println("contenido");
pw.close();


Escribir Objeto:
*Importantísimo el implements Serializable  en la clase del objeto


File in = new File(nomFitxer);

in.delete(); //eliminaria el fichero -> no hacer, es una nota
FileOutputStream fo = new FileOutputStream(nomFitxer, false);
ObjectOutputStream obo = new ObjectOutputStream(fo);
obo.writeObject(vf);
obo.close();
fo.close()



Leer objeto:

Factura [] vf = null;
FileInputStream fi = new FileInputStream(nomFitxer);
ObjectInputStream obi = new ObjectInputStream(fi);
vf = (Factura []) obi.readObject();
obi.close();
fi.close();
*/
    
}
